# ── Required ─────────────────────────────────────────────────────────────────

# Public IP of the load balancer that routes traffic to this node
LOAD_BALANCER_IP="203.0.113.10"

# PgBouncer pooled connection string (used for queries)
DATABASE_URL="postgresql://postgres:postgres@localhost:6432/zeitwork?sslmode=disable"

# Direct PostgreSQL connection string (used for WAL replication)
DATABASE_DIRECT_URL="postgresql://postgres:postgres@localhost:5432/zeitwork?sslmode=disable"

# This server's internal/VLAN IP address
INTERNAL_IP="10.0.0.1"

# Docker/OCI registry for storing built images
DOCKER_REGISTRY_URL="ghcr.io/yourorg"
DOCKER_REGISTRY_USERNAME="youruser"
DOCKER_REGISTRY_PAT=""  # GitHub PAT with write:packages scope

# ── GitHub App (optional) ────────────────────────────────────────────────────

GITHUB_APP_ID=""
GITHUB_APP_PRIVATE_KEY=""  # base64-encoded PEM key

# ── S3 / MinIO shared image storage (optional — only needed for multi-node) ─

S3_ENDPOINT="minio.internal:9000"
S3_BUCKET="zeitwork-images"
S3_ACCESS_KEY=""
S3_SECRET_KEY=""
S3_USE_SSL=false

# ── Edge proxy (optional — sensible defaults are built in) ───────────────────

# EDGEPROXY_HTTP_ADDR=":80"
# EDGEPROXY_HTTPS_ADDR=":443"
# EDGEPROXY_ACME_EMAIL="admin@zeitwork.com"
# EDGEPROXY_ACME_STAGING=false
